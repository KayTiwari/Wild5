import React, { Component } from 'react'
import { Text, View } from 'react-native'
import { Container, Label } from 'native-base'

export default class SleepQuest extends Component {
    render() {
        return (
            <Container>
                <Text> textInComponent </Text>
            </Container>
        )
    }
}
